import React from 'react';
import s from './Standart.module.scss'
const Standart = () => {
    return (
        <div className={s.Standart}>
            
        </div>
    );
};

export default Standart;